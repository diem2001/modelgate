services:
  modelgate:
    build: .
    network_mode: host
    volumes:
      - ./modelgate.config.yaml:/app/modelgate.config.yaml:ro
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - LMSTUDIO_API_KEY=${LMSTUDIO_API_KEY}
    restart: unless-stopped
